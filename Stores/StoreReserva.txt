DELIMITER //
CREATE PROCEDURE Reserva
(IN 
 	ID_mapa INT,
 	Clave INT,
 	FechaHoy date
	)
BEGIN

	DECLARE FechaDev date DEFAULT CURRENT_DATE();
    DECLARE estatus char(45) DEFAULT ('A tiempo');
                            
	insert into Prestamo (ID_libro,Estatus,Fecha_Prestamo,Fecha_Devolucion,Clave_Usuario)values(
        ID_mapa,
        estado,
        FechaHoy,
        FechaDev,
        Clave
	);
                            
END //